# agent-validation-suite runs fully offline and deterministically.
# No API keys are required. The placeholders below exist only to document
# where credentials would go if a future trajectory producer needed them.

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
